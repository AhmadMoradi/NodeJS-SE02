/*
const dirFltr = require('./mymodule');

const args = process.argv.slice(2);
const [dirPath, fileExt] = args;

dirFltr(dirPath, fileExt, (err, list) => {
    if (err) {
        console.error(err);
    }

    list.forEach(element => {
        console.log(element);
    });
});
*/